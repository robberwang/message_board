<h1>Editing Post</h1>
<% if !@post.image.nil? %>
  <div class="field">
    <%= image_tag @post.image.url(:thumb) %><br>
  </div>
<% end %>
<%= render 'form' %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
